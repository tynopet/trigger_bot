{
  "name": "telegram_trigger_bot",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node test.js",
    "build": "npm i && rm -rf public && mkdir public && cp -r node_modules public && cp -r handlers public && cp -r utils public && cp clients.js public && cp index.js public"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.3.1",
    "date-fns": "^1.28.5",
    "telegraf": "^3.17.8"
  }
}
